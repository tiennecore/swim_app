// src/graphql/resolvers.ts
import { Query } from './resolvers/Query';
import { Mutation } from './resolvers/Mutation';
import { Workout } from './resolvers/Workout';

export const resolvers = {
    Query,
    Mutation,
    Workout,
};